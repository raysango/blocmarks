<h1>New Topic</h1>
<%= form_for @topic do |f| %>
<div class = "form-group">
  <%= f.label :Title %>
  <%= f.text_area :title, class: 'form-control', placeholder: "Enter topic title" %>
</div>
<%= f.submit "Save", class: 'btn btn-success' %>
    <% end %>