<h1><%= @topic.title %></h1>
<%= link_to "Edit", edit_topic_path(@topic), class:"btn btn-success" %>
<%= link_to "Delete", @topic_path, method: :delete, class:"btn btn-danger" %>
<div class="row">
<% @topic.bookmarks.each do |bookmark| %>
<div class="col-md-2">
       <h4><%= bookmark.url %></h4>
     </div>
  <%end%>
</div>

